/**
 * 함수 내에서 사용한 return 은 return 뒤에 따라오는 값을 함수의 결과로 반환
 * 동시에 함수를 종료한다.
 */

function get_member1() {
    return 'gitflow';
}

function get_member2() {
    return 'mingooddev';
}

alert(get_member1());
alert(get_member2());